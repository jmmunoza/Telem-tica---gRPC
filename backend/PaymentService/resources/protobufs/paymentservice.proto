syntax = "proto3";

service PaymentService {
    rpc payOrder (Payment) returns (PaymentResponse);
}

message Payment {
    int32 order_id       = 1;
    float amount         = 2;
    string customer_name = 3;
}

message PaymentResponse {
    bool is_succesfull = 1;
}